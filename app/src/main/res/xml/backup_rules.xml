<?xml version="1.0" encoding="utf-8"?><!--
   Used on API 24–30 (ignored on API 31+, where data_extraction_rules.xml applies instead —
   see that file for why everything is included). Mirrors the same rules for older devices.
   See https://developer.android.com/guide/topics/data/autobackup for details.
-->
<full-backup-content>
    <include domain="database" path="." />
    <include domain="file" path="images/" />
    <include domain="sharedpref" path="." />
</full-backup-content>